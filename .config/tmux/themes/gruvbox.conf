set-option -g status-style bg=colour235,fg=colour223

set-window-option -g window-status-style bg=colour235,fg=colour223
set-window-option -g window-status-current-style bg=colour241,fg=colour223,bold

set-option -g pane-border-style fg=colour235,bg=colour235
set-option -g pane-active-border-style fg=colour136,bg=colour235 # Active pane border

# Message text colors (displayed on errors or status messages)
set-option -g message-style bg=colour235,fg=colour223

# # Set command-prompt colors (for command mode like prefix + :)
set-option -g message-command-style bg=colour235,fg=colour223
#
# # Clock mode colors
set-window-option -g clock-mode-colour colour223
#
# # Mode indicator colors (for copy mode, etc.)
set-option -g mode-style bg=colour240,fg=colour223

# set -g pane-border-fg colour235
# set -g pane-border-bg colour235
# set -g pane-active-border-fg colour136  # Gruvbox yellow
# set -g pane-active-border-bg colour235

# Message text colors
# set -g message-bg colour235
# set -g message-fg colour223

# Set command-prompt colors
# set -g message-command-fg colour223
# set -g message-command-bg colour235

# Clock mode colors
set -g clock-mode-colour colour223
# set -g clock-mode-bg colour235

# Mode indicator colors (for copy mode, etc.)
# setw -g mode-bg colour240

# Status line left side
set -g status-left-length 100
set -g status-left "#[fg=colour223,bg=colour241,bold] #S #[fg=colour241,bg=colour235,nobold,nounderscore,noitalics]"

# Status line right side
set -g status-right ""

# Set highlight color for active window
setw -g window-status-current-format "#[fg=colour223,bg=colour241,bold] #I:#W#[fg=colour241,bg=colour235,nobold,nounderscore,noitalics]"
setw -g window-status-format "#[fg=colour223,bg=colour235] #I:#W "

# Customize separator
set -g status-style "bg=colour235"
set -g window-status-separator ""
